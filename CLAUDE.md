# ErpApp

A Tigg-style ERP/CRM/Accounting rebuild for Nepali SMEs. Clean Architecture + CQRS (MediatR) on .NET 10 (LTS), Angular 21 (LTS) frontend, SQL Server via EF Core.

**Read `docs/roadmap.md` first** for what phase we're on and what's next. Full context lives in `docs/`:
- `docs/product-requirements.md` — the PRD (what the product does, for whom, why)
- `docs/architecture-spec.md` — bounded contexts, aggregates, cross-cutting engines (GL posting, document numbering, FIFO costing, authorization)
- `docs/erp-module-scan.md` — raw research: a live walkthrough of the reference product (Tigg) this rebuild is modeled on, module by module
- `docs/roadmap.md` — phased build plan, Phase 0 (done) through Phase 8+
- `docs/phase-0-status.md` — history of Phase 0: what was built, bugs hit and fixed, current status
- `docs/phase-1a-status.md` — history of Phase 1a: what was built, bugs hit and fixed (read this before touching auth/config wiring — several non-obvious gotchas), current status
- `docs/phase-1b-status.md` — history of Phase 1b: what was built, scope decisions, bugs hit and fixed, current status
- `docs/phase-1c-status.md` — history of Phase 1c: what was built, scope decisions (Role/RolePermission shape, permission marker interfaces), bugs hit and fixed, current status
- `docs/phase-2-status.md` — history of Phase 2: what was built (generic lookup CRUD, DocumentNumberingRule/IDocumentNumberGenerator, EAV CustomFieldDefinition/Value, TenantSettings real fields), scope decisions, bugs hit and fixed (read this before writing any raw-SQL EF Core query — several non-obvious `Database.SqlQuery<T>`/generic-LINQ-translation gotchas), current status
- `docs/phase-3-status.md` — history of Phase 3: what was built (Contact/ContactGroup, Product/ProductCategory/UnitOfMeasurement/ProductSecondaryUnit, record-detail-page Angular chrome), scope decisions (per-entity Member permission split, immutable Type, deferred FK stubs), bugs hit and fixed (read this before building any Angular component that handles both `.../new` and `.../:id` on the same route — a route-reuse gotcha), current status
- `docs/phase-4-status.md` — history of Phase 4: what was built (AccountGroup/Account, JournalVoucher as the first real ApprovableTransaction, IGlPostingRule/GlJournalEntry.Post GL posting engine, CashTransfer, the first Angular transactional-document chrome), scope decisions (permission-matrix split, no RowVersion threaded through commands, Product Account-FK backfill left unwired), bugs hit and fixed (read this before writing any handler that replaces an encapsulated child collection wholesale — a real EF Core InMemory-provider Clear+re-Add gotcha, plus a `Observable<A> | Observable<B>` TypeScript `.subscribe()` gotcha), current status
- `docs/phase-5-status.md` — history of Phase 5: what was built (Quotation/Invoice/Payment sales chain, document conversion via GetXConversionTemplateQuery, Warehouse lookup, TenantSettings default-GL-account fallback, SalesOrder+CreditNote stretch), scope decisions (TenantSettings-level GL account fallback, resolved-input-record posting rules, Warehouse kept minimal, Payment requires full allocation, SalesOrder shipped backend-only), bugs hit and fixed (read this before binding a native `<select>`'s `[value]` from a signal-fed array inside a freshly-created `@for` row — a real Angular option-vs-value race), current status
- `docs/phase-6-status.md` — history of Phase 6: what was built (PurchaseOrder/PurchaseBill/Expense/DebitNote purchase chain, TdsType lookup, Payment reused with Direction=Paid, TenantSettings Purchase-side default-GL-account fields), scope decisions (TDS reduces the AP credit rather than a separate line, Purchase VAT Receivable kept distinct from Sales VAT Payable, Payments.Payment.* permission kept shared across both Directions, DebitNote carries its own TdsTypeId/TdsAmount so a full reversal nets to zero — an earlier TDS-free design didn't), bugs hit and fixed (read this before binding *any* signal-fed `<select>`'s `[value]` — the `[value]`-vs-`@for` race isn't limited to freshly-created rows, two independent async subscribes resolving out of order can trigger it too; a latent Phase-5 `ListPaymentsQueryHandler` bug that only surfaced once Direction=Paid rows existed; a reversal-document posting rule that balanced on its own but left a paired account permanently unbalanced across the two related postings — read this before trusting `sum(Debit)==sum(Credit)` alone to prove a "reverse of X" document is correct; and a post-completion fix where every `ReferrerId`-carrying conversion document — Quotation→Invoice, PurchaseOrder→PurchaseBill, Invoice→CreditNote, PurchaseBill→DebitNote — turned out to have zero enforcement against its source beyond the FK itself, letting a source convert an unlimited number of times and a Credit/Debit Note credit/debit more quantity, a different Rate/VatRate, or a different Customer/Supplier/TDS Type than the source ever actually recorded — read this before building the next "Convert to X" flow, since none of that enforcement comes for free from the `ReferrerId` pattern itself), current status
- `docs/phase-7-status.md` — history of Phase 7: what was built (real FIFO `StockLedgerEntry`/`IStockLedgerService`, `FifoStockAvailabilityPolicy` replacing the always-Ok stub with a real Warn/Reject/OverrideWarning flow, real stock Increment/Consume wired into PurchaseBill/Invoice approval plus a new Invoice COGS GL leg, `WarehouseTransfer`/`InventoryAdjustment` document types, `ProductStockPositionQuery`/`InventoryLedgerQuery` backed by a new `StockMovement` audit entity), scope decisions (Invoice's COGS leg is a not-confirmed-live addition per `TenantSettings.DefaultInventoryAccountId`/`DefaultCogsAccountId`, `StockMovement` needed as a separate append-only entity since a mutable-in-place FIFO layer can't reconstruct OUT-side kardex history, the Warn-vs-never-go-negative tension between roadmap items 2 and 3 resolved as "override lets you attempt past a soft warning, it doesn't manufacture stock", `InventoryAdjustmentPostingRule` posts each direction's *total* as one pair rather than per-line so a mixed Increase+Decrease document still nets correctly), bugs hit and fixed (an `internal` Domain factory method that only compiles when the sole caller is same-assembly — Application-layer services calling a Domain entity's factory directly need `public`, not `internal`, unlike the "child line, called by its own parent aggregate" precedent; and a **live, currently-shipping instance of the `[value]`-vs-`@for` select race actually causing wrong data**, not just a display glitch — an Invoice's Warehouse field visually showed one warehouse while the persisted `WarehouseId` and the actual FIFO consumption were against a different one, confirmed via direct DB query — read this before trusting a signal-bound `<select>`'s on-screen value at all, on any page, without checking the DB), a **post-Phase-7 follow-up** (same doc, its own section) that closed CreditNote/DebitNote's FIFO-reversal gap (`InvoiceLine.CogsUnitCost` persisted at Invoice-approve time so a later CreditNote can put stock back at its exact original cost, matched by the same `(ProductId, Rate, VatRate)` triple the conversion-cap enforcement already uses) and fixed the three remaining `[value]`-vs-`@for` select instances, current status
- `docs/phase-8a-status.md` — history of Phase 8a: what was built (TrialBalanceQuery/BalanceSheetQuery/IncomeStatementQuery, pure `GlLine`/`GlJournalEntry` reads with no new commands/aggregates/migrations beyond a permission-seed-only one, `ITreeQuery<AccountGroup>` for Balance Sheet's group rollups), scope decisions (`ITreeQuery` is a portable in-memory BFS rather than architecture-spec.md §5's originally-recommended raw SQL Server recursive CTE — deliberately avoiding yet another `Database.SqlQuery<T>` composability gotcha, swappable later behind the same interface; all three reports filter on `GlJournalEntry.PostedAt`, not any document's own business `Date` field, an accepted-and-documented approximation until back-dated approvals prove it matters; Balance Sheet's `IsBalanced` invariant is computed straight from `Account.RootType` totals, decoupled from the group-rollup display so a mistagged AccountGroup tree can't silently corrupt it; all three report permission keys granted to both Admin and Member, following `InventoryLedgerView`'s precedent), no bugs hit this phase — read this before extending `ITreeQuery` to another tree entity or wiring a fourth report's date-cutoff logic, current status
- `docs/phase-8b-status.md` — history of Phase 8b: what was built (`SalesMasterReportQuery`/`PurchaseMasterReportQuery`, denormalized line-item fact tables over Invoice/CreditNote and PurchaseBill/DebitNote lines, no new commands/aggregates/schema tables beyond a permission-seed-only migration), scope decisions (both reports filter on each document's own business `Date` field, not `GlJournalEntry.PostedAt` — the opposite call from Phase 8a's three GL reports, since a document register needs the date a user actually picked, not an Approve-time posting timestamp; the two new `Reports.*MasterReport.View` permission keys are **Admin-only**, diverging from every other `Reports.*.View` key in this codebase, because a flat unaggregated per-line fact table sliceable across a tenant's entire history is a materially bigger exposure than a rollup or one document at a time; `ItemDiscount`/`TransactionDiscount`/`NetSales` columns from the reference product's confirmed shape are omitted entirely rather than shipped as always-zero placeholders, since no discount fields exist anywhere on `InvoiceLine`/`PurchaseBillLine`/etc.; CreditNote/DebitNote's `WarehouseId` resolves from their source Invoice/PurchaseBill via `ReferrerId`, `null` for a standalone reversal, reusing the same lookup Phase 7's FIFO-reversal fix already established; both report pages ship unpaginated, matching every other list page in this codebase, since no shared pagination component exists yet), bugs hit and fixed (both were test-authoring bugs, not defects in the shipped query handlers — asserting a document's *Create*-time `DraftCode` result instead of its *Approve*-time real code against a report row's `EntryNo`, and a shared-vs-fresh-`FakeDocumentNumberGenerator`-per-Approve-call bug that made every approved document in a test collide on the same `"{DocumentType}-0001"` code — read this before writing the next report-handler test suite that seeds real documents through their Create/Approve command handlers), current status
- `docs/phase-8c-status.md` — history of Phase 8c: what was built (`VatSummaryReportQuery` under `Application.Accounting.Queries.VatSummaryReport`, netting Approved Invoice/CreditNote and PurchaseBill/DebitNote lines into three `VatRate` buckets per side plus Output/Input VAT totals and a signed `NetVatPayable`, no new commands/aggregates/schema tables beyond a permission-seed-only migration), scope decisions (both sides filter on each document's own business `Date` field, same reasoning as Phase 8b's Master Reports, not `GlJournalEntry.PostedAt`; the new `Reports.VatSummary.View` permission key is **Admin+Member**, a deliberate return to Phase 8a's rollup precedent rather than Phase 8b's Admin-only flat-fact-table one, since this report's output is six netted numbers with no way to recover any single transaction's Rate/Contact/Product; the report's row/column shape is an explicitly-documented *designed* standard Nepal VAT-return structure rather than a reproduction of a confirmed Tigg screen, since `erp-module-scan.md` only names VAT Summary Report in the Tax Report category list and never opened it in the hands-on pass; all three `VatRate` buckets are always present and zero-filled on both sides rather than only emitting buckets with activity, matching a real VAT-return form's fixed row structure), no bugs hit in the shipped handler/endpoint/permission wiring this phase — read this before seeding a Goods-type test Product by hand or by script, since approving its Invoice hits two pieces of pre-existing Phase 7 behavior a Services-only seed (like Phase 8a/8b's own test data) never surfaces: a 422 stock-availability warning if no PurchaseBill created stock first, and a 409 if `TenantSettings.DefaultInventoryAccountId`/`DefaultCogsAccountId` aren't set, current status
- `docs/phase-8d-status.md` — history of Phase 8d: what was built (`TdsReportQuery` under `Application.Purchasing.Queries.TdsReport`, a deductee-wise flat TDS register over Approved PurchaseBill/Expense/DebitNote rows carrying a non-null `TdsTypeId`, no new commands/aggregates/schema tables beyond a permission-seed-only migration), scope decisions (filters on each document's own business `Date` field, same reasoning as Phase 8b/8c; DebitNote rows are listed as their own **negative-signed** row rather than netted into their source PurchaseBill's row, so a filing period shows that a reversal actually happened rather than silently absorbing it; the new `Reports.TdsReport.View` permission key is **Admin-only**, an explicit weighing of two independent factors — a flat per-contact fact table (Phase 8b's discriminator) *and* the first report in this codebase to surface a Contact's PAN, a real tax-ID field — that both point the same direction, unlike Phase 8c's rollup shape which pointed the other way; lives under `Application.Purchasing`, not `Application.Accounting` like VAT Summary, since TDS in this codebase only ever originates on the purchase side), no bugs hit in the shipped handler/endpoint/permission wiring this phase (one browser-automation mistake during manual E2E — a Contact-Type radio click that didn't register before submit, silently creating a Customer instead of a Supplier, caught via a direct DB query and fixed by creating a second Contact since `Contact.Type` is immutable per Phase 3 — not a codebase defect), current status
- `docs/phase-8e-status.md` — history of Phase 8e: what was built (`AnnexThirteenReportQuery` under `Application.Purchasing.Queries.AnnexThirteenReport`, a per-Contact Annex 13 rollup netting Approved Invoice/CreditNote (Sales) and PurchaseBill/Expense/DebitNote (Purchase) activity into six buckets — Service/Goods Purchase Capital/Others, Service/Goods Sales — filtered to Contacts whose total period activity meets an editable `ThresholdAmount` (100,000 NPR default), no new commands/aggregates/schema tables beyond a permission-seed-only migration), scope decisions (a DebitNote line's Capital-vs-Others classification resolves from its source PurchaseBill line via the established `(ProductId, Rate, VatRate)`-triple match, defaulting to Others for a standalone DebitNote; Expense activity — no `ProductId`, and despite `ExpenditureClassification`'s own doc comment, no `ExpenditureClassification` field either — is bucketed as `ServicePurchaseOthers`, since Expense is inherently non-goods and Others is that enum's own documented default; Opening/Closing Balance stay an explicit approximation — `Contact.OpeningBalance` as-is plus this row's own net period activity, not a real ledger balance, deferred to the future Ageing/Statement phase's running-balance engine; the new `Reports.AnnexThirteen.View` permission key is **Admin-only**, since every row is pinned to one specific Contact's identity including their PAN — the same PAN-exposure factor that made TDS Report Admin-only — even though the report's own shape is a per-Contact rollup like VAT Summary Report's Admin+Member one), one handler-robustness fix caught during design (a raw `ToDictionary` on the DebitNote-classification lookup key would throw if two lines on the same PurchaseBill legitimately shared a `(ProductId, Rate, VatRate)` key with different classifications — fixed by grouping first, same defensive pattern `PurchasingValidation.GetPurchaseBillRemainingByLineAsync` already uses for the same key) plus a recurrence of the `dotnet ef migrations add`-doesn't-apply-to-the-dev-database gotcha caught during manual E2E (not a codebase defect — a workflow step missed and then fixed with a plain `dotnet ef database update`), current status
## Stack & conventions
- Backend: .NET 10 (LTS), Clean Architecture (`src/Domain` → `src/Application` → `src/Infrastructure`/`src/Api`), CQRS via MediatR, FluentValidation, EF Core + SQL Server.
- Frontend: Angular 21 (LTS), in `web/`.
- Solution file is `ErpApp.slnx` (the new .NET 10 format, not `.sln`).
- Dependency rule: `Api → Application → Domain`; `Infrastructure → Application/Domain`. Nothing depends on `Infrastructure` or `Api` except `Api/Program.cs` (the composition root).
- Every command/query goes through the MediatR pipeline: `LoggingBehavior` then `ValidationBehavior` then `AuthorizationBehavior` (see `src/Application/Common/Behaviors/`). A command/query is only permission-gated if it implements `IRequirePermission` (`Application.Common.Security`) — most queries and Phase 0/1a/1b commands don't yet.
- Multi-tenancy: single database, shared schema, `OrganizationId` discriminator. No EF Core global query filter exists yet — every handler manually filters by `OrganizationId` in LINQ (e.g. `MyOrganizationsQueryHandler`, every Phase 2 lookup handler); introducing a shared `ITenantEntity`/`ApplyGlobalFilters<T>()` mechanism is a deliberately deferred, separate infra decision (would touch every existing Tenancy table too), not assumed to exist.
- Every transactional aggregate (Invoice, PurchaseBill, JournalVoucher, etc.) follows Draft → Approve lifecycle; document numbers are assigned **at Approve, not at Create** (confirmed live in the reference product — see `docs/erp-module-scan.md`'s Document Numbering section).

## Build & test commands
```
dotnet restore ErpApp.slnx
dotnet build ErpApp.slnx
dotnet test ErpApp.slnx          # Api.IntegrationTests needs Docker Desktop running (Testcontainers)
dotnet run --project src/Api     # Swagger at https://localhost:7104/swagger, health check at /health

cd web
npm ci
ng build
ng test --watch=false
ng serve                          # dev server, calls the API via web/src/environments/environment.development.ts
```

EF Core migrations:
```
dotnet ef migrations add <Name> --project src/Infrastructure --startup-project src/Api
dotnet ef database update --project src/Infrastructure --startup-project src/Api
```

Local SQL Server connection string, `Jwt:SigningKey`, and `Email:*` (SMTP) are all set via `dotnet user-secrets`, not in `appsettings.json` (see `src/Api/appsettings.Development.json` for the pointer commands). Never commit real values for any of these.

`gh` (GitHub CLI) is installed at `C:\Program Files\GitHub CLI\gh.exe` and authenticated as `lekhu-awasthi` — use it for PRs, CI log/artifact inspection, etc. If a fresh shell doesn't have it on `PATH`, invoke via the full path.

## Known gotchas (see docs/phase-0-status.md and docs/phase-1a-status.md for full history)
- MediatR 12.4.1's `RequestHandlerDelegate<TResponse>` is parameterless — call `next()`, not `next(cancellationToken)`, in pipeline behaviors.
- `dotnet ef` needs `Microsoft.EntityFrameworkCore.Design` referenced by whichever project is passed as `--startup-project` (`Api`), not just `Infrastructure`.
- **Never read configuration (`builder.Configuration.Get<T>()`, `.GetConnectionString(...)`, etc.) as a top-level statement in `Program.cs` before `.Build()` if the value gets captured into a closure that runs later** (an options-configure delegate, a DbContext options builder, `AddJwtBearer(options => ...)`, etc.) — that snapshot is taken too early to see config sources added afterward (notably `WebApplicationFactory`'s test-only overrides in `Api.IntegrationTests`), and the bug is easy to miss locally because developer-machine `user-secrets` are already loaded by the time the eager read happens, masking it. Bit us twice in Phase 1a (Jwt:SigningKey and ConnectionStrings:Default) — see `docs/phase-1a-status.md` item 8. Prefer `services.AddOptions<T>().Bind(configuration.GetSection(...))` (lazy) or resolve `IOptions<T>`/`IConfiguration` from `IServiceProvider` inside a lazily-invoked delegate.
- JWT bearer's default inbound claim mapping remaps `"sub"`/`"email"` to legacy XML-namespace claim types — set `options.MapInboundClaims = false;` or `ClaimsPrincipal.FindFirstValue(JwtRegisteredClaimNames.Sub)` silently returns null.
- Cookie `SameSite` must be `None` (not `Lax`), not just `Secure`, for the Angular dev server (`http://localhost:4200`) to receive it from the Api (`https://localhost:7104`) — differing scheme alone makes Chrome treat same-host requests as cross-site.
- `Response.Cookies.Delete(name, options)` only actually clears a cookie if `options` (`Path`/`Secure`/`SameSite`) matches what was used when the cookie was set — mismatched options are silently ignored by the browser.
- Validating a new migration by running `dotnet ef database update` against a scratch/Docker database (to sanity-check the generated SQL) does **not** apply it to the actual local dev database the Api's `ConnectionStrings:Default` user-secret points at — always follow up with a plain `dotnet ef database update` (no `--connection` override) before manually click-testing, or every endpoint that touches the new tables 500s with `Invalid object name`.
- `dotnet ef migrations add` orders operations by model diff, not by data safety — a migration that both drops a column and adds its replacement can scaffold the `DropColumn` *before* the `AddColumn`, silently losing the data needed to backfill the new one. Always read a scaffolded migration before applying it when a column's being replaced/retyped; reorder by hand (create/seed anything new first, backfill via raw SQL while the old column still exists, drop it last) — see `docs/phase-1c-status.md`'s bug #1 for a worked example (`OrganizationMembership.Role` string → `RoleId` FK).
- `dotnet ef migrations add` bundles the *entire* pending model diff into whichever migration you're adding — there's no way to scaffold two migrations from one model change without temporarily hiding part of the model from the DbContext. Plan for one migration per `migrations add` invocation, not per logical change; hand-review the single result instead of trying to force a split (see `docs/phase-2-status.md`'s bug #6).
- An EF Core enum property configured with `.HasDefaultValue(someValue)` where `someValue` differs from the enum's CLR `default` (its first-declared, "= 0" member) needs `.ValueGeneratedNever()` chained after it, or EF silently substitutes the SQL default on every insert whenever the in-memory value happens to equal `default(TEnum)` — even if a factory method explicitly chose that value. See `docs/phase-2-status.md`'s bug #2.
- `Database.SqlQuery<TResult>` (EF Core 8+) only accepts *composable* SQL (effectively a plain `SELECT`) — an `UPDATE ... OUTPUT` statement throws `InvalidOperationException` at query-translation time (not caught by `dotnet build`, only by actually running the query against a real provider). For an atomic read-and-increment, use an explicit transaction with `SELECT ... WITH (UPDLOCK, ROWLOCK)` followed by a separate `ExecuteSqlInterpolatedAsync` `UPDATE`, not a single `OUTPUT`-returning statement. Also: for a scalar `TResult`, the result set's column must be aliased `AS Value` — `SqlQuery<T>` binds by that column name, not positionally. See `docs/phase-2-status.md`'s bugs #3–4 (`DocumentNumberGenerator`).
- A MediatR handler generic over a type parameter constrained by an interface (e.g. `Handler<TLookup> where TLookup : ISomeInterface`) that accesses a property via that constraint (`x.SomeProperty`) risks EF Core's LINQ translator failing to map the *interface's* `PropertyInfo` back to the concrete entity's mapped column. Use `EF.Property<T>(x, nameof(ISomeInterface.SomeProperty))` instead. Also: such a handler's DI registration must target the closed generic `IRequestHandler<TRequest, TResponse>` (2 type args) explicitly — MediatR's assembly scan can't discover it, and a request implementing MediatR's bare `IRequest`/`IRequestHandler<T>` (1-arg convenience interfaces) may not satisfy the 2-arg registration's generic constraint depending on version; implement `IRequest<Unit>`/`IRequestHandler<T, Unit>` explicitly instead. See `docs/phase-2-status.md`'s bugs #1 and #5.
- An Angular component that handles both a `.../new` create route and a `.../:id` edit route (same route path, e.g. `contacts/:contactId`) must read the id from `route.paramMap` (an `Observable`) and re-derive its "is this new" state on every emission — Angular's default route-reuse strategy keeps the same component instance alive across that navigation, so a value captured once from `route.snapshot.paramMap` in the constructor goes stale the moment Create redirects to the new record's own URL (the page silently keeps showing the create form). See `docs/phase-3-status.md`'s bug #1.
- A `HttpClient.get<T>(url, { params })` call where `params`'s inferred type is a union including `{}` (e.g. `const params = cond ? { foo } : {};`) can fail TypeScript's overload resolution silently into the `responseType: 'arraybuffer'` overload, surfacing as a confusing `Observable<ArrayBuffer>` type error far from the real cause. Annotate `params: Record<string, string>` explicitly. See `docs/phase-3-status.md`'s bug #4.
- Assigning a ternary-chosen `request$: Observable<A> | Observable<B>` (Create vs Update, e.g. `const request$ = editingId ? updateX(...) : createX(...);`) fails the same way when `A`/`B` have different shapes — `.subscribe()` becomes `TS2349: This expression is not callable`, pointing at the call site rather than the real cause. Use an explicit `if (editingId) {...} else {...}` branch instead of a shared `request$` variable when the two result types differ. See `docs/phase-4-status.md`'s bug #3.
- An EF Core handler that replaces an *entire* encapsulated (private-backing-field) child collection in one save (`aggregate.ClearLines(); foreach (line) aggregate.AddLine(...);`) can get mis-tracked by the InMemory provider — a same-count Clear+re-Add was observed marking the wrong entries `Modified`/`Deleted` instead of `Added`/`Deleted`, throwing `DbUpdateConcurrencyException: ... does not exist in the store` on `SaveChangesAsync`, even with `.Include()`, `.IsRequired()`, and explicit `.OnDelete(Cascade)` all in place. Don't rely on collection-navigation-triggered fixup for a full-collection replace — snapshot the old children first and explicitly `db.Set<TChild>().RemoveRange(oldChildren)` / `.AddRange(newChildren)` around the mutation. See `docs/phase-4-status.md`'s bug #1 (`UpdateJournalVoucherCommandHandler`/`UpdateCashTransferCommandHandler`).
- A native `<select>`'s `[value]` property binding can silently lose to its own `@for`-generated `<option>` children when both the select and its options are created fresh in the same change-detection pass (e.g. a multi-line editable table's row keyed by an ever-incrementing counter, so a reload assigns every row a brand-new `@for` key) — Angular applies the select's own `[value]` binding before the nested `@for`'s `<option>` elements have their `value` attributes set, so the browser can't match anything and silently falls back to `selectedIndex = 0`, even though the underlying bound data is correct (computed totals derived from the same data will show the right answer while the `<select>` visually shows the wrong option — that mismatch is the tell). Selects whose options come from a signal that already resolved in an earlier, unrelated change-detection cycle (e.g. populated by a separate subscribe in the constructor) don't show the bug, which can make it look field-specific rather than structural. Fix: don't bind `[value]` on the `<select>` at all — bind `[selected]="option === boundValue"` on each `<option>` instead, so the correct option marks itself selected independent of the parent's binding-evaluation order. See `docs/phase-5-status.md`'s bug #1.
- The same `[value]`-vs-`@for` race above isn't limited to freshly-created-row selects — a **static, top-level** `<select>` (e.g. a header-level Warehouse/TDS-Type picker, not inside a per-line `@for`) can hit it too if the signal driving `[value]` and the signal driving the `@for` options resolve via two *independent* async subscribes that happen to finish out of order: if the bound-value signal updates before the options signal has rendered its `<option>`s, the browser drops the unmatched `select.value` write, and Angular's signal-based fine-grained reactivity does **not** retrigger that `[value]` binding later just because the unrelated options signal changed (it only re-runs bindings that read the signal that changed). "Options come from an early constructor subscribe" is not a reliable guarantee against this — it happened to hold in Phase 5's call graph but isn't structural. Default to `[selected]` per-option for every select fed by a signal, not only ones inside a freshly-created `@for` row. See `docs/phase-6-status.md`'s bug #1.
- Before assuming a MediatR query handler's `Where` clause matches its own request's fields, actually read it — `ListPaymentsQueryHandler` shipped in Phase 5 with a hardcoded `x.Direction == PaymentDirection.Received` filter that has no corresponding field on `ListPaymentsQuery` at all, invisible for an entire phase because Phase 5 only ever created `Direction=Received` rows. A handler whose only observed behavior always happens to match its hardcoded assumption is exactly the kind of bug manual E2E passes miss and a second call site (here, Phase 6's first `Direction=Paid` payment) exposes immediately. See `docs/phase-6-status.md`'s bug #2.
- A posting rule for a "reverse of X" document type (e.g. DebitNote reversing PurchaseBill, CreditNote reversing Invoice) can satisfy `GlJournalEntry.Post`'s own `sum(Debit)==sum(Credit)` balanced-invariant while still leaving a *paired* account permanently unbalanced once you look at the combined effect of the original document plus its reversal — e.g. `DebitNotePostingRule` debited Accounts Payable for the full grand total while the source `PurchaseBillPostingRule` had only ever credited AP net of TDS (TDS was posted to a separate TDS Payable line instead), so a 100%-matching reversal left AP off by exactly the TDS amount and TDS Payable stuck forever, even though both documents' own entries balanced individually and every automated test passed. Don't trust "this rule's own entry is balanced" as proof a reversal is correct — trace the *net* effect on every account the original document touched, across both postings, especially any account (like a TDS/withholding control account) that the reversing document doesn't obviously need to know about. See `docs/phase-6-status.md`'s bug #3.
- Setting `ReferrerType`/`ReferrerId` on a new document (the architecture-spec.md §3.3 conversion pattern — Quotation→Invoice, Invoice→CreditNote, PurchaseOrder→PurchaseBill, PurchaseBill→DebitNote) enforces nothing by itself — it's an informational FK, not a guard. Every `CreateXCommandHandler` behind a "Convert to X" button needs its own explicit check, written by hand, or: (1) the source document can be converted an unlimited number of times (add a status like `Converted` to the source and a domain `MarkConverted()` method, called from the new document's Create handler, not just checked at Approve); (2) a reversal-target document (CreditNote/DebitNote) can credit/debit more quantity than the source ever recorded, or claim a Rate/VatRate combination that never existed on any source line (key remaining-quantity by the *exact* `(ProductId, Rate, VatRate)` triple, net of prior non-Void reversals against the same source — not by ProductId alone, and not just a "don't exceed the max rate" ceiling, which still lets VAT/Rate float free below it); (3) it can point at a different Contact or tax-type field (TDS Type, etc.) than the source actually used, which reproduces the reversal-imbalance gotcha above through a different field entirely. See `docs/phase-6-status.md`'s bug #4 for the four-part fix retrofitted onto all four already-shipped conversion flows.
- A Domain entity's factory/mutator method only needs to stay `internal` when its *only* caller is another Domain type in the same assembly (e.g. a child line's `Create` called from its own parent aggregate's `AddLine`). The moment an Application-layer service constructs or mutates that entity *directly* — not through a parent aggregate's own public method — `internal` doesn't cross the Domain/Application assembly boundary and the call site fails to compile (`CS1061`/`CS1501`, caught immediately by `dotnet build`, not a runtime surprise). `StockLedgerEntry.Consume`/`StockMovement.Create` hit this in Phase 7 (their only real caller, `IStockLedgerService`, lives in Application) and had to be `public` instead. See `docs/phase-7-status.md`'s bug #1.
- The `[value]`-vs-`@for` native-`<select>` race (documented above from Phase 5/6) isn't just a cosmetic "wrong option shown" bug — confirmed in Phase 7 to cause **actually-wrong persisted data**, silently. An Invoice's Warehouse `<select>` (still using the old `[value]="warehouseId()"` pattern, never retrofitted after the gotcha was first documented) visually showed one warehouse throughout an entire create→save→approve session while a direct DB query proved the row's real, persisted `WarehouseId` — and the actual FIFO stock consumption — was against a *different* warehouse the whole time. Never trust a signal-bound `<select>`'s on-screen value as proof of what got saved, on any page, without checking the database; audit every remaining `[value]`-on-`<select>` instance in the codebase for this, not just the ones a current phase happens to touch. See `docs/phase-7-status.md`'s bug #2.

## Current status
**Phase 8e (Annex 13 Report) is complete** — `AnnexThirteenReportQuery` (`Application.Purchasing.Queries.AnnexThirteenReport`) is a pure read producing a per-Contact Annex 13 rollup: one row per Contact with any Approved Sales or Purchase activity in the period, netting Invoice/CreditNote and PurchaseBill/Expense/DebitNote into six buckets — Service Purchase Capital/Others, Goods Purchase Capital/Others, Service Sales, Goods Sales — the confirmed field list from `erp-module-scan.md` line 279, filtered to rows whose total activity meets an editable `ThresholdAmount` (100,000 NPR default). No new commands/aggregates/schema tables beyond a permission-seed-only migration (`AddPhase8eReportPermissions`). A DebitNote line's Capital-vs-Others classification resolves from its source PurchaseBill line via the same `(ProductId, Rate, VatRate)`-triple match Phase 6's conversion-cap enforcement established, defaulting to Others for a standalone DebitNote (scope decision #1); Expense activity — which carries neither a `ProductId` nor (despite `ExpenditureClassification`'s own doc comment claiming otherwise) an `ExpenditureClassification` field — is bucketed as `ServicePurchaseOthers`, Expense being inherently non-goods and Others being that enum's own documented default (scope decision #2); Opening/Closing Balance stay an explicit approximation — `Contact.OpeningBalance` as-is, `ClosingBalance = OpeningBalance + TotalActivity`, Payment allocations excluded — deferred to the future Ageing/Statement phase's real running-balance engine, the same "revisit later" framing Phase 8a used for its own `PostedAt`-not-business-`Date` approximation (scope decision #3). The new `Reports.AnnexThirteen.View` permission key is **Admin-only** — an explicit re-derivation against this report's own shape rather than a copy of either precedent: Annex 13's output *is* a per-Contact rollup like VAT Summary Report's (which earned Admin+Member), but unlike VAT Summary, every row is pinned to one specific Contact's identity including their PAN, the same PAN-exposure factor that made TDS Report Admin-only — that factor isn't diluted just because the shape is a rollup (scope decision #4). Angular gets one new read-only report page (`annex-thirteen-report-page` under `features/reports/`) with a date-range picker plus an editable Threshold Amount input, one flat table, no totals footer (each row is already a per-Contact total), and a dashboard nav link next to TDS Report's. Confirmed by hand end-to-end: a fresh Admin set up a Chart of Accounts/Warehouse/Goods+Service Products/a Customer and two Suppliers (one with a 5,000 Opening Balance, one deliberately small), approved a four-line PurchaseBill (one line per bucket), a DebitNote reversing part of its Goods-Capital line, an Expense, a small PurchaseBill on the second Supplier, an Invoice with a Goods and a Service line, and a standalone CreditNote reversing part of the Goods line, then pulled the Annex 13 Report through the real UI against the real API/DB with `ThresholdAmount=1000` — exactly two rows, every bucket and the Closing Balance matching hand arithmetic exactly, the small-activity Supplier correctly excluded by the threshold; a second user invited as Member hit the same report and got the real API's `403` naming `Reports.AnnexThirteen.View`, shown cleanly in the page's own error banner, confirming the Admin-only grant. `dotnet build`/`dotnet test` (67 Domain.UnitTests unchanged, 126 Application.UnitTests — 2 new + 124 pre-existing — and 4 Api.IntegrationTests, all green, Docker Desktop running this session) and `ng build`/`ng test` (7 pre-existing specs green, no new Angular specs) all pass. See `docs/phase-8e-status.md` for the full history (all four scope decisions in detail, a handler-robustness fix caught during design — a raw `ToDictionary` that would throw on a legitimate duplicate key, fixed by grouping first — and a recurrence of the `dotnet ef migrations add`-doesn't-apply-to-the-dev-database gotcha caught during manual E2E).

**Phase 8d (TDS Report) is complete** — `TdsReportQuery` (`Application.Purchasing.Queries.TdsReport`) is a pure read producing a deductee-wise TDS register: one row per Approved PurchaseBill/Expense/DebitNote carrying a non-null `TdsTypeId` (a document with no TDS deduction is excluded entirely, not shown zero-filled), filtered on each document's own business `Date` field within the query range — the same non-GL reasoning Phase 8b/8c established. No new commands/aggregates/schema tables beyond a permission-seed-only migration (`AddPhase8dReportPermissions`). Unlike VAT Summary Report's bucketed rollup, this is a flat register — a real TDS return is filed deductee-wise, needing each Contact's name/PAN and each document's own withheld amount — so it lives under `Application.Purchasing` (not `Application.Accounting`, since TDS in this codebase only ever originates on the purchase side, never Sales). A DebitNote reversing a PurchaseBill's TDS is listed as its own **negative-signed row** (`GrossAmount`/`TdsAmount`/`NetPayableAmount` all negated) rather than netted into the PurchaseBill's row, so a filing period visibly shows that a reversal happened (scope decision #1); the totals footer still nets correctly since the negative row sums straight in. The new `Reports.TdsReport.View` permission key is **Admin-only** — an explicit weighing of two independent factors that both point the same direction here (a flat per-contact fact table, Phase 8b's own discriminator, *and* the first report in this codebase to expose a Contact's PAN, a real tax-ID field), unlike Phase 8c where the rollup shape argued the opposite way (scope decision #2). Angular gets one new read-only report page (`tds-report-page` under `features/reports/`) with a date-range picker only, one flat table (Entry Date/Type/Entry No/Contact/PAN/TDS Type/Rate %/Gross/TDS/Net Payable), a totals footer, and a dashboard nav link next to VAT Summary Report's. Confirmed by hand end-to-end: a fresh Admin set up a Chart of Accounts/Warehouse/Supplier-with-PAN/Service Product/TDS Type, approved a PurchaseBill (gross 1130, TDS 150) and an Expense (gross 500, TDS 75), converted 3 of the PurchaseBill's 10 units to a DebitNote and approved it (gross 339, TDS 45), and pulled the TDS Report through the real UI against the real API/DB — three rows exactly as expected, the DebitNote showing `-339.00`/`-45.00`/`-294.00`, and a totals footer of Gross 1291.00/TDS 180.00/Net 1111.00 matching hand arithmetic exactly; a second user invited as Member hit the same report and got the real API's `403` naming `Reports.TdsReport.View`, confirming the Admin-only grant. `dotnet build`/`dotnet test` (67 Domain.UnitTests unchanged, 124 Application.UnitTests — 2 new + 122 pre-existing, all green; `Api.IntegrationTests` not re-run this session since Docker Desktop wasn't running, the same gap Phase 5 hit) and `ng build`/`ng test` (7 pre-existing specs green, no new Angular specs) all pass. See `docs/phase-8d-status.md` for the full history (both scope decisions in detail, plus a browser-automation mistake hit during manual E2E — not a codebase defect — worth knowing before driving Contact creation through browser automation again).

**Phase 8c (VAT Summary Report) is complete** — `VatSummaryReportQuery` (`Application.Accounting.Queries.VatSummaryReport`) is a pure read producing a standard Nepal VAT-return-style summary: net Approved Invoice lines minus Approved CreditNote lines, and net Approved PurchaseBill lines minus Approved DebitNote lines, both bucketed by the three `VatRate` values (`NoVat`/`ZeroVat`/`ThirteenPercentVat`) and both filtering on each document's own business `Date` field — not `GlJournalEntry.PostedAt` — the same non-GL reasoning Phase 8b's Master Reports established (scope decision #1). No new commands/aggregates/schema tables beyond a permission-seed-only migration (`AddPhase8cReportPermissions`). Unlike Phase 8b's Master Reports, this report's output is a rollup — three buckets per side plus Output/Input/Net VAT totals, never a single transaction's Rate/Contact/Product — so the new `Reports.VatSummary.View` permission key is **Admin+Member**, deliberately returning to Phase 8a's precedent rather than continuing Phase 8b's Admin-only one (scope decision #2, an explicit judgment call, not a silent default either way). `erp-module-scan.md` never opened Tigg's actual VAT Summary Report screen in its hands-on pass — it's only named in the Tax Report category card list — so this phase's row/column shape is an explicitly-documented *designed* standard VAT-return structure (three `VatRate` buckets × Net Amount + VAT Amount per side, plus totals), not a claimed reproduction of a confirmed live screen (scope decision #3). All three `VatRate` buckets are always present and zero-filled when a rate has no activity in the period, on both sides, rather than only emitting buckets with data — a VAT-return-style summary is read against a fixed "what's my 13%, what's my 0%-rated, what's exempt" mental model, so a silently-missing zero bucket would read as a bug (scope decision #4). Angular gets one new read-only report page (`vat-summary-report-page` under `features/reports/`) with a date-range picker only — no Contact/Product/Warehouse filters, since this is a filing-period summary, not a transaction register — two small tables (Sales-by-VatRate, Purchase-by-VatRate), and a Net VAT Payable/Refundable summary line whose sign is surfaced as-is, never clamped to zero. Confirmed by hand end-to-end: a fresh Admin set up a Chart of Accounts (including Inventory/COGS accounts, needed here because — unlike Phase 8a/8b's Service-only test data — this phase's manual E2E used a Goods-type Product to better match a real Nepali retail SME's VAT-return scenario) plus a Customer/Supplier/Warehouse/VAT-rated Product, approved two PurchaseBills and two Invoices (one of each rate) plus a standalone CreditNote and DebitNote both against the `ThirteenPercentVat` bucket, and pulled the VAT Summary Report through the real UI against the real API/DB — every bucket's Net Amount and VAT Amount matched hand arithmetic exactly, and the Net VAT Refundable badge correctly showed 13.00 (Output VAT 26.00 − Input VAT 39.00 = −13.00) without being clamped to zero. `dotnet build`/`dotnet test` (67 Domain.UnitTests, 122 Application.UnitTests — 2 new + 120 pre-existing, 4 Api.IntegrationTests, all green) and `ng build`/`ng test` (7 pre-existing specs green) all pass. See `docs/phase-8c-status.md` for the full history (all four scope decisions in detail, plus two pieces of pre-existing Phase 7 product behavior — a stock-availability 422 and a missing-COGS-account 409 — that the manual E2E pass hit while seeding a Goods Product for the first time in the Reports sequence, neither a defect in this phase's own code).

**Phase 8b (Sales & Purchase Master Reports) is complete** — `SalesMasterReportQuery`/`PurchaseMasterReportQuery` (`Application.Sales.Queries.SalesMasterReport`/`Application.Purchasing.Queries.PurchaseMasterReport`) are pure reads producing a denormalized line-item fact table over Invoice/CreditNote and PurchaseBill/DebitNote lines respectively — no new commands/aggregates/schema tables beyond a permission-seed-only migration (`AddPhase8bReportPermissions`), matching Phase 8a's "pure read" framing. Unlike Phase 8a's three GL reports, both queries filter on each document's own business `Date` field (`Invoice.Date`, `PurchaseBill.Date`, etc.), not `GlJournalEntry.PostedAt` — a document register needs the date a user actually picked when creating the document, not an Approve-time posting timestamp, and every relevant aggregate already carries that field so no cross-cutting change was needed to use it (see the status doc's scope decision #1). Two new permission keys (`Reports.SalesMasterReport.View`/`Reports.PurchaseMasterReport.View`) are **Admin-only** — a deliberate divergence from every other `Reports.*.View` key in this codebase (Phase 8a's three, `InventoryLedgerView`), since a flat unaggregated per-line fact table sliceable by Contact/Product/Warehouse across a tenant's *entire* transaction history is a materially bigger exposure of Rate/margin-adjacent data than a rollup or one document at a time (scope decision #2). `ItemDiscount`/`TransactionDiscount`/`NetSales` columns from the reference product's confirmed live shape are omitted entirely — no discount fields exist anywhere on `InvoiceLine`/`CreditNoteLine`/`PurchaseBillLine`/`DebitNoteLine` in this codebase, so inventing always-zero placeholder columns would silently imply discount support that doesn't exist (scope decision #3). CreditNote/DebitNote carry no `WarehouseId` column of their own; each report resolves it from the source Invoice/PurchaseBill via `ReferrerId` when present (reusing the same lookup Phase 7's FIFO-reversal fix already established), `null` for a standalone reversal (scope decision #4). Angular gets two new read-only report pages (`sales-master-report-page`/`purchase-master-report-page` under `features/reports/`) with date-range pickers plus optional Contact/Product/Warehouse filter dropdowns (all `<select>`s use `[selected]` per-option, never `[value]` on the `<select>` itself) and an unpaginated flat table — no pagination component exists anywhere in this codebase yet, so both reports ship flat rather than introducing new shared UI infrastructure inside a deliberately small phase (scope decision #5). Confirmed by hand end-to-end: a fresh Admin set up Chart of Accounts/Customer/Supplier/Warehouse/VAT-rated Product, approved a PurchaseBill, converted a Quotation to an Invoice and sold part of the PurchaseBill's stock, issued a CreditNote against the Invoice and a DebitNote against the PurchaseBill, and pulled both Master Reports through the real UI against the real API/DB — each showed exactly the expected two rows (source document plus its reversal, correct Warehouse resolved on both including the reversal rows), every Contact/Product/Warehouse filter combination narrowed correctly, and a Member account got a 403 on both report endpoints confirming the Admin-only grant. `dotnet build`/`dotnet test` (120 Application.UnitTests, 6 new + 114 pre-existing, all green) and `ng build`/`ng test` (7 pre-existing specs green) all pass. See `docs/phase-8b-status.md` for the full history (scope decisions and the two test-authoring bugs hit and fixed along the way — a Create-vs-Approve-time document-code mix-up and a shared-vs-fresh `FakeDocumentNumberGenerator` collision, neither touching the shipped query handlers themselves).

**Phase 8a (Core Financial Reports) is complete** — `TrialBalanceQuery`/`BalanceSheetQuery`/`IncomeStatementQuery` (`Application.Accounting.Queries`) are pure reads over `GlLine`/`GlJournalEntry` joined to `Account`/`AccountGroup`, no new commands/aggregates/schema tables, matching the roadmap's own "pure GL queries" framing for this phase. Trial Balance nets every active Account's Debit/Credit onto its natural side as of a cutoff date and surfaces a live `IsBalanced` check (JournalVoucher's "Difference: Rs. 0" pattern, generalized). Balance Sheet groups Asset/Liability/Equity accounts by top-level `AccountGroup` with a full-subtree rollup via the new `ITreeQuery<AccountGroup>` (an in-memory BFS, not architecture-spec.md §5's originally-recommended raw SQL Server recursive CTE — see the status doc's scope decision #1 for why), adds a synthetic "Net Income (Current Period)" plug line under Equity since there's no period-close/retained-earnings posting anywhere in this codebase, and computes its `IsBalanced` invariant (`Assets == Liabilities + Equity`) straight from `Account.RootType` totals, decoupled from the group-rollup display so a mistagged AccountGroup tree can't silently corrupt it. Income Statement nets Income-minus-Expense activity within a date range. All three queries filter on `GlJournalEntry.PostedAt` (the Approve-time posting timestamp), not any document's own business `Date` field — an explicit, documented approximation (scope decision #2), not silently baked in — since threading a business date through every `IGlPostingRule<TDocument>` call site across Sales/Purchasing/Accounting/Inventory is a real cross-cutting change out of scope for this deliberately small phase. Three new `Reports.{TrialBalance,BalanceSheet,IncomeStatement}.View` permission keys, granted to both Admin and Member (following `Inventory.InventoryLedgerView`'s precedent). Angular gets three read-only report pages (`trial-balance-page`/`balance-sheet-page`/`income-statement-page`) with date/date-range pickers and dashboard nav links — no `<select>` elements on any of them, so the repeated `[value]`-vs-`@for` gotcha simply doesn't apply this phase. Confirmed by hand end-to-end: a fresh Admin set up a Chart of Accounts with a doubly-nested Asset subgroup (Current Assets → Cash & Bank → Cash in Hand), approved three JournalVouchers, and pulled all three reports through the real UI against the real API/DB — Trial Balance balanced at 15,000/15,000, Balance Sheet correctly rolled the nested Cash account up into its grandparent group and balanced at Assets 13,000 = Liabilities 2,000 + Equity 11,000 (including the 1,000 Net Income plug), Income Statement showed Net Income 1,000, and changing the Trial Balance's As Of Date to before any postings correctly zeroed every row. See `docs/phase-8a-status.md` for the full history (scope decisions — the `ITreeQuery` in-memory-BFS-vs-raw-CTE call, the PostedAt-not-business-Date approximation, the decoupled `IsBalanced` computation, the Admin+Member permission grant — no bugs hit this phase, a genuinely clean "pure read" build).

**Phase 7 (Inventory & stock ledger) is complete** — `StockLedgerEntry` (FIFO layer) and `IStockLedgerService`/`StockLedgerService` (Increment/Consume/GetAvailableQuantity/PreviewConsumptionCost) replace Phase 5/6's `AlwaysOkStockAvailabilityPolicy` stub with the real thing; `FifoStockAvailabilityPolicy` branches on `TenantSettings.NegativeStockBalanceAction` (Reject/Warn/DoNothing), and a genuine confirmable warning (`StockAvailabilityWarningException`, HTTP 422, distinct from `ConflictException`'s 409) lets Invoice approval's `OverrideWarning` flag resubmit without a second round trip. `ApprovePurchaseBillCommandHandler` now creates a real FIFO layer per Goods line; `ApproveInvoiceCommandHandler` now really consumes those layers and — a not-confirmed-live scope decision, see the status doc — posts a second Debit COGS/Credit Inventory GL leg using the FIFO-computed cost, against two new `TenantSettings` fields (`DefaultInventoryAccountId`/`DefaultCogsAccountId`) since Product carries no per-Product override for either. Two new document types: `WarehouseTransfer` (Consume one warehouse, Increment another at the carried-over FIFO cost — the only `ApprovableTransaction` in this codebase with no GL posting at all) and `InventoryAdjustment` (mixed Increase/Decrease lines in one document, posts GL against a new `DefaultInventoryAdjustmentAccountId` variance account, net effect worked out on paper first per Phase 6 bug #3's lesson before writing the posting rule). `ProductStockPositionQuery`/`InventoryLedgerQuery` round out the query side, the latter backed by a small new `StockMovement` audit entity (a FIFO layer alone can't reconstruct kardex OUT-history once partially consumed by more than one document). Confirmed by hand end-to-end: a fresh Admin can set up Chart of Accounts/two Warehouses/Supplier/Customer/VAT-rated Product, approve a PurchaseBill (FIFO layer created, Stock Position shows the balance), approve a WarehouseTransfer (stock splits across warehouses, no GL), approve an Invoice (COGS posted correctly, GL balanced), approve an InventoryAdjustment with both an Increase and a Decrease line in one document (GL nets correctly), and confirm the Inventory Ledger/kardex reconciles exactly — all through the real UI against the real API/DB, plus the Warn/Reject/override decision tree confirmed directly against the API (422 on first over-large approve, 409 on override-retry once the shortfall is real, 409 immediately under Reject). See `docs/phase-7-status.md` for the full history (scope decisions — the COGS-leg addition, the new `StockMovement` entity, the Warn-vs-never-go-negative resolution, `InventoryAdjustmentPostingRule`'s per-direction-total posting — and bugs hit and fixed, including a `public`-vs-`internal` Domain-factory cross-assembly gotcha and a **live, currently-shipping instance of the `[value]`-vs-`@for` select race actually causing wrong persisted data**, not just a display glitch, on Invoice's Warehouse field — read this bug #2 before trusting any signal-bound `<select>`'s on-screen value anywhere in this codebase without checking the database). **Post-Phase-7 follow-up (same status doc):** CreditNote now reverses an Invoice's FIFO consumption (restocking at the exact cost `InvoiceLine.CogsUnitCost` recorded, plus the matching Debit-Inventory/Credit-COGS GL leg) and DebitNote now reverses a PurchaseBill's FIFO increment, both gated on `ReferrerType` actually pointing at the expected source — a standalone Credit/Debit Note still touches no stock at all; the three remaining `[value]`-vs-`@for` selects (Quotation/Payment/JournalVoucher) were also fixed. Verified end-to-end against a real DB: PurchaseBill 10 units in → Invoice sells 4 → CreditNote returns 2 (stock 10→6→8, GL Debit Inventory 200/Credit COGS 200 confirmed) → DebitNote returns 3 to the supplier (stock 8→5, no spurious GL leg) → a standalone CreditNote left stock untouched.

**Phase 6 (Purchase chain) is complete** — `PurchaseOrder` → `PurchaseBill` → Supplier `Payment` (Direction=Paid) live end-to-end, plus `Expense` (account-based lines, no Product) and `DebitNote` (full-stack, as the PurchaseBill-conversion target). `PurchaseBillPostingRule`/`DebitNotePostingRule` are the first posting rules with a TDS leg (TDS reduces the Accounts Payable credit rather than posting as a separate unrelated line, and `DebitNotePostingRule` mirrors that exactly so a full reversal nets AP and TDS Payable to zero — an earlier TDS-free DebitNote design didn't, see the gotcha below); `Payment`'s existing aggregate/handlers gained a real `Direction`-aware branch (Received vs Paid) instead of a second command/handler pair, confirming Phase 5's "near-zero-new-code" prediction. `TdsType` is a new Configuration lookup (Code/Name/RatePct, reusing the generic lookup pair); `TenantSettings` grew four Purchase-side default-GL-account fields (`DefaultPurchaseAccountId`/`DefaultAccountsPayableId`/`DefaultVatReceivableAccountId`/`DefaultTdsPayableAccountId`, deliberately a distinct VAT Receivable account from Sales' VAT Payable — a real accounting distinction, not a naming one) alongside Phase 5's three, all edited from the same extended "Accounting Defaults" Angular page. `GetPurchaseBillConversionTemplateQuery`/`GetDebitNoteConversionTemplateQuery` reuse architecture-spec.md §3.3's conversion pattern for the third/fourth time. Confirmed by hand end-to-end: a fresh Admin can set up Chart of Accounts/Warehouse/TdsType/Supplier/VAT-rated Product, create and approve a PurchaseOrder, convert it to a PurchaseBill with TDS applied and approve (GL posts Debit Purchase Expense/VAT Receivable, Credit TDS Payable/Accounts Payable net of TDS, balanced), record and approve a Supplier Payment against it (FIFO-suggested allocation, GL posts Debit AP/Credit Cash — exact mirror of Customer Payment's posting), create and approve an Expense with TDS applied (GL posts directly against its line Accounts plus AP/TDS Payable), and convert the PurchaseBill to a Debit Note and approve (GL posts the exact reverse, TDS leg included — confirmed by hand that Accounts Payable and TDS Payable both net to exactly zero across a full-reversal PurchaseBill+DebitNote pair), all through the real UI against the real API/DB. `Api.IntegrationTests` was run against the new migration with Docker Desktop actually running this time (the thing Phase 5 deferred). See `docs/phase-6-status.md` for the full history (scope decisions — TDS-reduces-AP-credit, Purchase-vs-Sales VAT account split, `Payments.Payment.*` kept shared across both Directions, DebitNote carrying its own TDS fields — and bugs hit and fixed, including a second, differently-triggered instance of the native-`<select>`-vs-`@for` race, a latent Phase-5 `ListPaymentsQueryHandler` bug only Phase 6's first Direction=Paid row exposed, and a reversal-posting-rule bug where each document's own entry balanced individually but the *combined* effect on Accounts Payable/TDS Payable didn't — worth reading before trusting any "reverse of X" posting rule just because it satisfies its own balanced-invariant).

**Post-Phase-6 fix (both Phase 5 and Phase 6 conversion flows):** every conversion document turned out to have no enforcement against its source beyond the `ReferrerId` FK — Quotation/PurchaseOrder could be converted an unlimited number of times, and Credit Notes/Debit Notes could credit/debit more quantity than was ever invoiced/billed, or freely change Rate/VAT/Customer/Supplier/TDS Type away from what the source document actually recorded. Fixed with a `Converted` status on Quotation/PurchaseOrder (one conversion per source), an exact-`(ProductId, Rate, VatRate)`-line-match cap on Credit/Debit Note quantities (net of prior non-Void reversals, multiple *partial* reversals still allowed by design), and a Contact/TDS-Type match check — enforced server-side and mirrored in the Angular UI (Product/Rate/VAT/Contact/Reference/TDS-Type fields disabled on any Credit/Debit Note carrying a `ReferrerId`, only Quantity and line-removal stay editable). See `docs/phase-6-status.md`'s bug #4 for the full write-up. Next up: **Phase 7 — Inventory & stock ledger**. See `docs/roadmap.md`'s Phase 7 section for the full task breakdown before starting.

Phase 5 (Sales chain) is complete — `Quotation` → `Invoice` → Customer `Payment` (Direction=Received) live end-to-end, plus `SalesOrder` (backend-only) and `CreditNote` (full-stack, as the Invoice-conversion target) as the stretch goal. `Invoice` is the first real use of `IGlPostingRule<TDocument>` for a non-JournalVoucher/CashTransfer type and the first aggregate with a required `WarehouseId`; `GetInvoiceConversionTemplateQuery`/`GetCreditNoteConversionTemplateQuery` implement architecture-spec.md §3.3's document-conversion pattern (server-computed pre-fill, ordinary Create command POSTed afterward) for the first time. `Warehouse` (minimal single-column lookup) and `TenantSettings`'s three new default-GL-account fields (`DefaultSalesAccountId`/`DefaultAccountsReceivableId`/`DefaultVatPayableAccountId`, editable via a new "Accounting Defaults" Angular page) give Invoice/CreditNote's posting rules a fallback when a Product has no `SalesAccountId` of its own. Angular clones `journal-voucher-detail-page`'s transactional-document chrome for Quotation/Invoice/CreditNote/Payment (Product picker + Qty/Rate/VAT columns, running Line/VAT/Grand Total, two-step Draft-save vs Approve, read-only GL Transactions section, plus a "Convert to X" button on Approved source documents and a live GL-preview-before-approve on Payment). Confirmed by hand end-to-end: a fresh Admin can set up a Chart of Accounts/Warehouse/Accounting Defaults/Customer/VAT-rated Product, create and approve a Quotation, convert it to an Invoice and approve (GL posts Debit AR/Credit Sales Revenue/Credit VAT Payable, balanced), record and approve a Customer Payment against it (FIFO-suggested allocation, GL posts Debit Cash/Credit AR, balanced), and convert the Invoice to a Credit Note and approve (GL posts the exact reverse), all through the real UI against the real API/DB. See `docs/phase-5-status.md` for the full history (scope decisions — TenantSettings-level GL-account fallback, resolved-input-record posting rules for Invoice/CreditNote, Payment requiring full allocation, SalesOrder's Angular UI deliberately cut — and bugs hit and fixed, including a real Angular native-`<select>`-vs-`@for`-options race worth knowing before binding any select's `[value]` inside a freshly-created row).

Phase 4 (Accounting core) is complete — `AccountGroup` (self-referencing tree, 5 root types) + `Account` (leaf, auto-numbered) under a new `accounting` schema; `JournalVoucher` is the first real `ApprovableTransaction` (Draft → Approve, `IDocumentNumberGenerator` called from `Approve()` for the first time, not `Create()`); the shared GL posting engine (`IGlPostingRule<TDocument>` + `GlJournalEntry.Post()`) is built and proven against both `JournalVoucher` and `CashTransfer` (a fan-out simplified UI over the same posting path). Angular gets its first "transactional document" chrome — a multi-line editable Debit/Credit table with a live client-side Total/Difference indicator and an explicit two-step Draft-save vs Approve action — which Sales/Purchase (Phase 5+) will clone. Confirmed by hand: a fresh Admin can create an AccountGroup for each of the 5 root types, an Account under one, a JournalVoucher with 2 balanced lines (Draft → Approve, real number assigned, GL Transactions section populated), and a CashTransfer with a 2-destination fan-out (Draft → Approve, GL posts as one balanced entry), all through the real UI against the real API/DB. See `docs/phase-4-status.md` for the full history (scope decisions — permission-matrix split with maker-checker seed data, no RowVersion threaded through commands, Product's deferred Account-FK columns backfilled but left unwired — and bugs hit and fixed, including a real EF Core InMemory-provider Clear+re-Add gotcha worth knowing before writing the next "replace this whole child collection" handler).

Phase 3 (Contacts & Catalog) is complete — `Contact` (Customer/Supplier/Lead, auto-numbered) and `Product` (Goods/Service, auto-numbered) aggregates, `ContactGroup`/`ProductCategory` self-referencing trees and `UnitOfMeasurement` (all three reusing Phase 2's generic `ListLookupsQuery<TLookup>`/`DeleteLookupCommand<TLookup>` pair), `ProductSecondaryUnit` child collection with `AddSecondaryUnit` (new `contacts`/`catalog` schemas). Angular establishes the record-detail-page chrome (left mini-profile panel + vertical tab list + right content pane) for the first time, alongside list-page chrome, for both Contacts and Products. Confirmed by hand: a fresh Admin can create a ContactGroup, a Contact of each Type, a UnitOfMeasurement, a ProductCategory, and a Product of each Type (including a secondary unit) through the real UI against the real API/DB. See `docs/phase-3-status.md` for the full history (scope decisions — per-entity Member permission split, immutable Type, deferred Account/PrintProfile FK stubs — and bugs hit and fixed, including an Angular route-reuse gotcha worth knowing before building the next detail-page screen).

Phase 2 (Configuration foundation) is complete — generic `ListLookupsQuery<TLookup>`/`DeleteLookupCommand<TLookup>` CRUD plus concrete Create/Update for `CreditTerm`/`PaymentMode`/`CustomStatus`/`ReportingTagCategory`/`ReportingTagOption` (new `configuration` schema), `TenantSettings`' real fields, `DocumentNumberingRule`/`IDocumentNumberGenerator` (race-safe under concurrent callers, integration-tested against real SQL Server), and `CustomFieldDefinition`/`CustomFieldValue` (EAV, definition CRUD only). Angular Configurations screens exist for CreditTerm and PaymentMode. Confirmed by hand: a fresh Admin can create/edit/delete both through the real UI against the real API/DB. See `docs/phase-2-status.md` for the full history (scope decisions, the `Database.SqlQuery<T>` composability gotchas, bugs hit and fixed).

Phase 1c (Minimal role/permission stub, Identity/Tenancy context) is complete — `Role`/`RolePermission` tables (two system-level roles, Admin/Member) and a real `AuthorizationBehavior` MediatR pipeline behavior replace the ad hoc admin checks Phase 1b inlined in `InviteUserCommandHandler`/`AcceptRequestCommandHandler`. Confirmed by hand: a fresh Admin can still create an Organization and invite a Member; a Member calling the invite endpoint directly gets a real HTTP 403. See `docs/phase-1c-status.md` for the full history (scope decisions on system-level vs. per-org roles, the `MembershipRole`-as-selector design, bugs hit and fixed).

Phase 1b (Organization & membership, Tenancy context) is complete — full login → create Organization via 3-step wizard → land on its dashboard → invite a second user → accept from that user's account flow confirmed working by hand, backed by real SQL Server persistence and real SMTP invite email. See `docs/phase-1b-status.md` for that history.

Phase 1a (User & auth, Identity context) is complete — CI green on PR [#1](https://github.com/lekhu-awasthi/ERP/pull/1) (`feature/phase-1a-identity-auth`). See `docs/phase-1a-status.md` for that history.
