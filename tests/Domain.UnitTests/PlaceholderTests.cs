namespace ErpApp.Domain.UnitTests;

/// <summary>
/// Phase 0 harness proof: Domain has no business logic yet, so this just confirms
/// the test project builds, references Domain, and xunit runs. Delete once Phase 1
/// adds real Domain entities with real unit tests.
/// </summary>
public class PlaceholderTests
{
    [Fact]
    public void Harness_is_wired_up()
    {
        Assert.True(true);
    }
}
