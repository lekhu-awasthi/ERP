{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "// Default": "Do NOT put a real connection string here for local dev. Use user-secrets instead:",
    "// Setup": "dotnet user-secrets set \"ConnectionStrings:Default\" \"Server=localhost,1433;Database=ErpApp;User Id=sa;Password=<yours>;TrustServerCertificate=True\" --project src/Api"
  },
  "Jwt": {
    "// SigningKey": "Do NOT put a real signing key here. Use user-secrets instead:",
    "// Setup": "dotnet user-secrets set \"Jwt:SigningKey\" \"<a long random string>\" --project src/Api"
  },
  "Email": {
    "// All": "Do NOT put real SMTP credentials here. Use user-secrets instead:",
    "// Setup": "dotnet user-secrets set \"Email:From\" \"<address>\" --project src/Api (repeat for SmtpServer/Port/Username/Password)"
  },
  "AlertScheduler": {
    "// Enabled": "Set false to run the app without the Phase 20e alert-email loop (e.g. a second app instance, or a laptop you do not want sending mail). Defaults true.",
    "// PollInterval": "How often the scheduler asks what is due. Defaults to 00:01:00, which matches the HH:mm resolution of the product's own time picker.",
    "// ManualTesting": "Shorten it for a manual end-to-end run without waiting for a real slot: dotnet user-secrets set \"AlertScheduler:PollInterval\" \"00:00:15\" --project src/Api (it is read through IOptionsMonitor, so a change is picked up on the next tick without restarting)"
  },
  "Turnstile": {
    "// SecretKey": "Do NOT put a real Cloudflare secret key here. Use user-secrets instead:",
    "// Setup": "dotnet user-secrets set \"Turnstile:SecretKey\" \"<your secret key>\" --project src/Api",
    "// LocalDev": "Cloudflare's always-passes dummy secret key for local dev is 1x0000000000000000000000000000000AA (pairs with the always-passes dummy sitekey 1x00000000000000000000AA already baked into web/src/environments)"
  }
}
