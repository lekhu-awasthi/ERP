namespace ErpApp.Domain.Tenancy;

/// <summary>
/// Hardcoded stub per roadmap Phase 1c ("just enough to unblock later phases") -- the real
/// Role/RolePermission catalog with a per-document-type permission matrix lands later.
/// </summary>
public enum MembershipRole
{
    Admin = 0,
    Member = 1,
}
